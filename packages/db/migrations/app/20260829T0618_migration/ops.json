[
  {
    "id": "alterNullability.dropNotNull.user.photo",
    "label": "Drop NOT NULL on \"user\".\"photo\"",
    "operationClass": "widening",
    "target": {
      "id": "postgres",
      "details": {
        "schema": "public",
        "objectType": "column",
        "name": "photo",
        "table": "user"
      }
    },
    "precheck": [
      {
        "description": "ensure column \"photo\" exists",
        "sql": "SELECT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "user",
          "photo"
        ]
      }
    ],
    "execute": [
      {
        "description": "drop NOT NULL on \"photo\"",
        "sql": "ALTER TABLE \"public\".\"user\" ALTER COLUMN \"photo\" DROP NOT NULL"
      }
    ],
    "postcheck": [
      {
        "description": "verify column \"photo\" is nullable",
        "sql": "SELECT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3 AND \"is_nullable\" = $4)) AS \"result\"",
        "params": [
          "public",
          "user",
          "photo",
          "YES"
        ]
      }
    ]
  }
]